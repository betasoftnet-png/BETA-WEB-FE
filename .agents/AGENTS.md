# Project Rules

- Do not execute any git commands or perform git operations (such as git add, git commit, git push, git pull, git fetch, git checkout, etc.) without the user's explicit permission and knowledge.
